/**
 * A list of supported mongodb operators to be cast based on schemas.
 */
export const castableOperators = [
  "$eq",
  "$ne",
  "$lt",
  "$lte",
  "$gt",
  "$gte",
  "$exists",
  "$size",
  "$in",
  "$nin",
  "$all",
  "$elemMatch",
  "$and",
  "$or",
  "$not",
  "$nor",
];
